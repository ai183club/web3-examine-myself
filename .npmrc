# The desktop workspace may run pnpm without an interactive TTY. Avoid an
# automatic dependency purge before every script; dependency installation is
# kept explicit in the documented pnpm install step.
verify-deps-before-run=off
verifyDepsBeforeRun=off
